import type { Meta, StoryObj } from "@storybook/react";
import RealProperty from "./RealProperty";

const meta: Meta<typeof RealProperty> = {
  title: "RealProperty",
  component: RealProperty,
};

export default meta;
type Story = StoryObj<typeof RealProperty>;

export const Primary: Story = {
};
